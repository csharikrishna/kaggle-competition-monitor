version: '3.8'

services:
  kaggle-monitor:
    build: .
    container_name: kaggle-competition-monitor
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "10000:10000"
    volumes:
      - ./data:/app/data

